<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Fail Page</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <h1>Login Failed</h1>
    </body>
</html>


<!DOCTYPE html>
<html lang="en">
    <head>
        <title>View Movies</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>

        <h1>Find a movie in the database</h1>
        <form action="/movies/foundmovie" method= "GET">
        <!-- <label for="movie">Movie</label><br/> -->
        <input type="text" id="title" name= "title" placeholder="Movie title"/><br/>
        
        <button type="submit">Find a movie</button>
        </form>
        <h1>Movies</h1>
        <% if (title) {
             %>
               <h2> <%= `${movies[m].title[0].toUpperCase()}${movies[m].title.slice(1)}` 
               %>
        </h2>
        <p><%=title.rating %></p>
        <p><%=title.synopsis %></p>
        <p><%=title.releaseyear %></p>
        <p><%=title.genre %></p>
        <p><%=title.director %></p>
        <p><%=title.boxoffice %></p>
        <img src="<%=title.movieposter %>"></img>
        <hr/>
        <% } %>
    </body>
</html>